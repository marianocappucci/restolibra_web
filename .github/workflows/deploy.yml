name: Deploy a VPS

# Reusable workflow compartido con Contalibra/Gestiolibra/MedLibra/
# VentaLibra -- extraído 2026-07-26, ver
# wiki/analyses/auditoria-duplicacion-familia-libra.md (P3, continuación).

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: marianocappucci/libra-web-kit/.github/workflows/deploy-vps.yml@main
    with:
      remote_path: /root/restolibra_web/
    secrets: inherit
